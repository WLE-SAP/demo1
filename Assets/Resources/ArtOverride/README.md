# 图片素材上传文件夹（美术替换 · 一物一图）

把图片直接拖进这个文件夹，游戏启动时会自动用它替换「这一个物件」的程序化图元。

**规则：文件夹里没有图片 → 用代码画的图元（默认状态）；有图片 → 图片优先。一张图只管一个 key。**

> 这里是快捷说明；**完整格式要求与像素建议见根目录 [`README.md`](../../../README.md)**。

---

## 1. 最重要的一条：一物一图

以前是「一张 `disc.png` 同时换掉树冠 + 村民的头 + 羊 + 路灯……」，现在**每个物件一个 key**：

- `house_roof.png` 只换屋顶，**不会**连带换掉木箱、村民身子、农田；
- 想换哪样就放哪样，其余的保持程序化美术，互不影响；
- 没放的、没写对名字的，都会在 Console 里如实报出来。

## 2. 文件命名规则

文件名（不含扩展名）= key。比较时**忽略大小写、下划线、短横线和空格**，
所以 `HouseRoof.png`、`house_roof.png`、`house-roof.png` 是同一个 key。

同一个 key 放了多张图 → **只用先载入的那张**，其余的会在 Console 里被点名。

## 3. key 一览（游戏里）

「九宫格」= 会按物体大小拉伸，四角不跟着变形（导入时自动把边框按原素材比例放大到你的图上）。

| key | 是什么 | 占地 | 推荐像素 | 说明 |
| --- | --- | --- | --- | --- |
| `bug_head` | 小虫的头 | 1 × 1 | 128 × 128 | 朝向**默认朝右**（+X 是正前方）；长大时等比放大，别贴边 |
| `crosshair` | 点击移动的标记 | 1 × 1 | 64 × 64 | 居中的十字 / 圆圈 |
| `highlight` | 高亮底衬（食物 / 果实） | 1 × 1 | 64 × 64 | 正圆、铺满画布；**会被黄 / 橙两档颜色染色** |
| `highlight_rect` | 高亮底衬（木箱） | 1 × 1 | 64 × 64 | 九宫格圆角矩形；同样会被染色 |
| `ground` | 地面草地（**草原**地貌的地面） | 4 × 4 | 256 × 256 | **必须无缝平铺**，用 2 的幂尺寸；导入时自动设成 Repeat |
| `road` | 道路 / 广场（**农村**） | 4 × 4 | 256 × 256 | **必须无缝平铺**；走向按图内纹理 |
| `ground_forest` | 地面（**森林**地貌） | 4 × 4 | 256 × 256 | **必须无缝平铺**；没图时用 `ground` 的图染成深绿 |
| `ground_desert` | 地面（**沙漠**地貌） | 4 × 4 | 256 × 256 | **必须无缝平铺**；没图时用 `road` 的图染成沙黄 |
| `road_dirt` | 土路（**荒野**） | 4 × 4 | 256 × 256 | **必须无缝平铺**；荒野的路更窄 |
| `road_paved` | 石铺路（**城市**） | 4 × 4 | 256 × 256 | **必须无缝平铺**；城市的路更宽 |
| `house_roof` | 房屋屋顶 | 1 × 1 | 128 × 128 | 九宫格。屋顶大小每栋不一样，四角要留够不变形区 |
| `house_wall` | 房屋墙体 | 1 × 1 | 64 × 64 | 九宫格 |
| `house_door` | 房屋门 | 1 × 1 | 64 × 64 | 九宫格 |
| `house_window` | 房屋窗 | 1 × 1 | 64 × 64 | 九宫格；**白天偏蓝、夜里变暖黄**（代码染色） |
| `house_chimney` | 烟囱（房屋 / 作坊共用同一张） | 1 × 1 | 64 × 64 | 九宫格 |
| `shop_sign` | 作坊招牌（面包房 / 铁匠铺共用） | 1 × 1 | 64 × 64 | 九宫格 |
| `anvil` | 铁砧 | 1 × 1 | 64 × 64 | |
| `forge` | 铁匠炉火 | 1 × 1 | 64 × 64 | **夜里会更亮更大**（代码染色） |
| `villager_body` | 村民的身子 | 1 × 1 | 64 × 64 | **会被职业颜色染色**（白图 = 直接用职业色） |
| `villager_head` | 村民的头 | 1 × 1 | 64 × 64 | **会被肤色染色** |
| `tree_canopy` | 树冠 | 1 × 1 | 128 × 128 | 给了图就不再叠程序化的「内部高光」 |
| `berry` | 地上的果子 | 1 × 1 | 64 × 64 | 留 1–2px 透明边 |
| `leaf` | 地上的叶子 | 1 × 1 | 64 × 64 | 留 1–2px 透明边 |
| `special_food` | 神奇果实 | 1 × 1 | 128 × 128 | 给了图就不再叠内部亮斑与小星星（外圈光晕保留） |
| `farm_soil` | 农田的土地 | 1 × 1 | 128 × 128 | 九宫格 |
| `farm_row` | 农田的垄 | 1 × 1 | 64 × 64 | |
| `farm_sprout` | 农田的幼苗 | 1 × 1 | 64 × 64 | |
| `pen_grass` | 牧场草地 | 1 × 1 | 128 × 128 | 九宫格 |
| `fence` | 畜栏围栏 | 1 × 1 | 64 × 64 | |
| `sheep` | 羊的身子 | 1 × 1 | 64 × 64 | |
| `sheep_head` | 羊的头 | 1 × 1 | 64 × 64 | |
| `garden_bed` | 花坛的土 | 1 × 1 | 128 × 128 | 九宫格 |
| `garden_edge` | 花坛的边 | 1 × 1 | 64 × 64 | |
| `garden_flower` | 花坛里的花 | 1 × 1 | 64 × 64 | **会被 5 种花色随机染色** |
| `well_rim` | 水井的井圈 | 1 × 1 | 128 × 128 | |
| `well_water` | 水井的水面 | 1 × 1 | 128 × 128 | |
| `well_post` | 水井的立柱 | 1 × 1 | 64 × 64 | |
| `stall_counter` | 摊位的柜台 | 1 × 1 | 64 × 64 | |
| `stall_awning` | 摊位的遮阳篷 | 1 × 1 | 64 × 64 | **会被 5 种篷布色随机染色** |
| `stall_post` | 摊位的立柱 | 1 × 1 | 64 × 64 | |
| `stall_goods` | 摊位上的货物 | 1 × 1 | 64 × 64 | |
| `bench_seat` | 长椅的坐板 | 1 × 1 | 64 × 64 | |
| `bench_back` | 长椅的靠背 | 1 × 1 | 64 × 64 | |
| `board_post` | 公告板的柱子 | 1 × 1 | 64 × 64 | |
| `board` | 公告板的板面 | 1 × 1 | 64 × 64 | |
| `board_paper` | 公告板上的纸 | 1 × 1 | 64 × 64 | |
| `lamp_post` | 路灯的灯柱 | 1 × 1 | 64 × 64 | |
| `lamp_head` | 路灯的灯头 | 1 × 1 | 64 × 64 | **夜里会发亮**（代码染色） |
| `burrow_rim` | 地洞的洞口边 | 1 × 1 | 128 × 128 | |
| `burrow_hole` | 地洞的洞 | 1 × 1 | 128 × 128 | |
| `burrow_inner` | 地洞的深处 | 1 × 1 | 128 × 128 | |
| `burrow_stone` | 地道旁的小石头 | 1 × 1 | 64 × 64 | |
| `crate` | 木箱 | 1 × 1 | 64 × 64 | 九宫格；木箱大小随机 |
| `battery` | 旧电池（吃了解锁【电击】） | 0.30 × 0.30 | 64 × 64 | |
| `trash` | 破布团（吃了解锁【伪装】） | 0.32 × 0.32 | 64 × 64 | |
| `spore` | 孢子囊（吃了解锁【分裂】） | 0.30 × 0.30 | 64 × 64 | 给了图就不再叠内部高光点 |
| `gear` | 锈齿轮（吃了解锁【抖动】） | 0.28 × 0.28 | 64 × 64 | |
| `acid` | 酸液瓶（吃了解锁【腐蚀】） | 0.30 × 0.30 | 64 × 64 | |
| `wire` | 电线（啃断 → 附近断电） | 0.55–0.75 | 64 × 64 | 被剪断后会被代码染暗 |
| `pump` | 抽水泵（断电 → 漏水） | 0.9–1.1 | 128 × 128 | 九宫格；断电后变灰+微缩 |
| `oilbarrel` | 油桶（被电击/打碎 → 爆炸） | 0.75–0.9 | 128 × 128 | 九宫格 |
| `alarm` | 警报器（响 → 全村撤离） | 0.5–0.6 | 64 × 64 | 响的时候被染成红色闪 |

## 3.5 key 一览（按「聚落 × 自然体系」分家的内容）

世界现在由两套体系组成：**自然体系**（草原 / 森林 / 沙漠）决定长什么样，
**聚落体系**（荒野 / 农村 / 城市）决定有多热闹。下面的 key 只在特定的地貌 / 聚落里出现。

**不同聚落的房子**（`house_roof` / `house_wall` / `house_door` / `house_window` 是所有房型共用的墙、门、窗）

| key | 是什么 | 占地 | 推荐像素 | 说明 |
| --- | --- | --- | --- | --- |
| `house_upper_wall` | 两层小楼 / 排屋 / 公寓的**上层墙** | 1 × 1 | 64 × 64 | 九宫格 |
| `apartment_roof` | 公寓的平屋顶 | 1 × 1 | 128 × 128 | 九宫格 |
| `apartment_window` | 公寓的小窗（成排） | 1 × 1 | 64 × 64 | 九宫格 |
| `barn_roof` | 谷仓的大屋顶 | 1 × 1 | 128 × 128 | 九宫格 |
| `barn_door` | 谷仓的双开大门 | 1 × 1 | 128 × 128 | 九宫格 |
| `cabin_wall` | 木屋的圆木墙 | 1 × 1 | 64 × 64 | 九宫格 |
| `cabin_roof` | 木屋的屋顶 | 1 × 1 | 128 × 128 | 九宫格 |

**聚落地标**

| key | 是什么 | 占地 | 推荐像素 | 说明 |
| --- | --- | --- | --- | --- |
| `windmill_body` | 风车的塔身（含顶盖） | 1.8–2.4 × 2.6–3.4 | 128 × 128 | 九宫格；四片叶片会慢慢转 |
| `windmill_blade` | 风车的叶片 | 0.26 × 2.1 | 64 × 128 | 四片共用一张；画成竖直的一条 |
| `bell_tower` | 钟楼的塔身（含顶） | 1.6–2.0 × 3.6–4.6 | 128 × 128 | 九宫格；整点会敲钟 |
| `bell` | 钟楼里的钟 | 0.62 | 64 × 64 | 响的时候周围会亮一下 |
| `fountain_rim` | 喷泉的池边 | 3.6 | 128 × 128 | 城市广场的中心 |
| `fountain_water` | 喷泉的水面（中央水柱也用它） | 2.8 / 0.55 | 128 × 128 | |
| `campfire_stone` | 篝火的石头 | 0.36 | 64 × 64 | 围成一圈各画一块 |
| `campfire_fire` | 篝火的火苗 | 0.95 | 64 × 64 | **夜里更亮更大**（代码染色） |

**按自然体系换的景物与食物**

| key | 是什么 | 占地 | 推荐像素 | 说明 |
| --- | --- | --- | --- | --- |
| `bush` | 灌木（草原 / 森林） | 0.8–1.35 | 96 × 96 | 会给一点淡淡的影子 |
| `cactus_body` | 仙人掌主干（沙漠） | 0.36–0.5 × 1.1–1.9 | 64 × 128 | |
| `cactus_arm` | 仙人掌的侧枝 | 0.3 × 0.5 | 64 × 64 | 一张图会横竖用两次 |
| `dead_tree` | 枯树（沙漠多、森林少） | 0.2 × 1.2–2.0 | 64 × 128 | 树干与枝共用一张 |
| `oasis_water` | 绿洲的水面（沙漠稀有） | 2.8–3.6 | 128 × 128 | |
| `palm_trunk` | 棕榈树干 | 0.2 × 1.6–2.4 | 64 × 128 | |
| `palm_leaf` | 棕榈树叶 | 0.85 | 128 × 128 | 围成一圈各画一片 |
| `mushroom` | 蘑菇（**森林**的食物） | 0.30 | 64 × 64 | 长在树根附近 |
| `pinecone` | 松果（**森林**的食物） | 0.26 | 64 × 64 | 长在树底下 |
| `cactus_fruit` | 仙人掌果（**沙漠**的食物） | 0.30 | 64 × 64 | 长在仙人掌旁边 |
| `wheat` | 麦穗（**草原**的食物） | 0.32 | 64 × 64 | 草原上到处都是 |

**按聚落 / 自然换的可交互物**

| key | 是什么 | 占地 | 推荐像素 | 说明 |
| --- | --- | --- | --- | --- |
| `rock` | 岩石（沙漠 / 草原） | 0.55–0.85 | 96 × 96 | 能搬（很沉）、能撞碎 |
| `hay_bale` | 草垛（草原） | 0.8–1.0 | 96 × 96 | 九宫格；能搬能吃 |
| `log` | 木料堆（森林） | 0.8–1.05 | 96 × 96 | 九宫格；能搬能吃 |
| `trash_can` | 垃圾桶（城市） | 0.7–0.9 | 96 × 96 | 九宫格；能搬能吃 |
| `bucket` | 木桶（农村 / 城市的草原与森林） | 0.55–0.75 | 64 × 64 | 九宫格；能搬能吃 |
| `lantern` | 灯笼（农村 / 城市） | 0.4–0.5 | 64 × 64 | 能搬走；**夜里会亮**（代码染色） |

> 想知道哪个 key 在哪片地貌出现，看根目录 [`README.md`](../../../README.md) 的表；
> 生成规则本身在 `Assets/Scripts/WorldBiome.cs` / `ContentPack.cs`。

## 4. key 一览（开始界面）

| key | 是什么 | 推荐像素 | 说明 |
| --- | --- | --- | --- |
| `menu_background` | 整屏背景 | 1920 × 1080 或任意 16:9 | **整张铺满屏幕**（不重复平铺），画一张完整的背景图即可 |
| `menu_panel` | 面板底板（主面板 / 设置） | 128 × 128 | 九宫格圆角矩形；**会被面板颜色染色**（白图 = 米白面板） |
| `menu_button` | **所有按钮**共用的底图 | 128 × 128 | 九宫格圆角矩形；**会被各按钮自己的颜色染色**（开始=绿、继续=蓝…） |

> 不放这三个 key 时，开始界面**自己用代码画**：背景是纵向渐变（深绿 → 稍亮的绿），
> 面板与按钮是白色圆角矩形九宫格，再乘上各自的颜色 —— 所以永远不会有空白或报错。

## 5. 颜色：什么时候图片会被染色

| 情况 | 行为 |
| --- | --- |
| 绝大多数 key | **图片原样显示**（颜色刷成白色，程序化配色被顶掉） |
| `villager_body` / `villager_head` | 保留染色：衣服颜色 = 职业、肤色随机 —— 颜色本身是「这个村民是谁」的信息 |
| `house_window` / `lamp_head` / `forge` | 保留染色：白天 / 夜里颜色不同（夜里会亮起来） |
| `highlight` / `highlight_rect` / `garden_flower` / `stall_awning` | 保留染色：底衬的黄 / 橙、花的 5 色、篷布的 5 色 |
| `menu_panel` / `menu_button` | 保留染色：颜色是「哪个按钮干什么」的标识 |

**想完全控制外观**：给上面这些 key 就画成白色 / 浅灰的形状图（染色后就是你要的颜色），
或者干脆不改这些物件（它们本身就有配色）。

## 6. 尺寸、锚点与九宫格（导入时自动算好）

导入时 `ArtOverridePostprocessor` 会把图片设成 **Sprite**，并做两件事：

1. 按 **该 key 对应的原素材在世界里的宽度** 反推 `Pixels Per Unit`
   → **不管你是 64px 还是 1024px，替换后占的位置都和原来一样大**；
2. 九宫格 key 会把**边框**按原素材的圆角比例自动放大到你的图上
   → 你的图四角不会被拉伸变形（例：`menu_button` 64px 原图圆角 20px，你交 128px 的图 → 边框自动设成 40px）。

- **锚点固定为图片中心** → 内容画在正中间，四周留透明边距；
- 比例按**宽度**对齐，高度按图片自身长宽比等比缩放；
- **平铺 key**（`ground` / `road` / `ground_forest` / `ground_desert` / `road_dirt` / `road_paved`）
  导入时会自动把 Wrap Mode 设成 **Repeat**（否则平铺时接缝处会被拉伸出一道糊边）；
- 想手动改大小：改该图片 Import Settings 里的 `Pixels Per Unit`（越大画面里越小）。
  自动配置只在**首次导入**时执行，之后你改的值会保留；
- 文件名没对上 key 时按通用值导入：**64 像素 = 1 世界单位**，并会在 Console 里被点名。

## 7. 生效范围与限制

- 只影响 `BugScene` 与 `MainMenu`；小虫的尾巴是 LineRenderer 画的曲线，**没有 key 换不掉**
  （要调它改 `Bug/Body` 上 WormBody 的 `width` / `spacing` / 摆动幅度）。
- 世界是无限流式的：区块边走边生成 / 回收，但同类物件用的是同一个 key 的图，替换是全局一致的。
- 场景里手工摆的三个精灵（`Bug/Head`、`MoveMarker`、`Environment/Ground`）通过 `ArtSlot` 组件标 key；
  其余物件在生成时自己套用。

## 8. 怎么看有没有生效

运行游戏后看 Console：

- `[ArtOverride] 从 Resources/ArtOverride 载入 N 张图片。` → 图片被读进来了
- `[ArtOverride] 图片 N 张，场景精灵替换了 M 个（村庄里的物件在生成时已各自替换）。` → 生效
- `没对上 key（名字写错了？）：xxx` → 文件名没对上，照上面的表改名
- `key xxx 放了多张图片，只用先载入的 …` → 同一个 key 放多了，删到只剩一张
- `[ArtOverride] 开始界面：背景 用图片 / 程序绘制渐变，面板 x/y 用图片，按钮 x/y 用图片。` → 开始界面
- `[ArtOverride] Resources/ArtOverride 里没有图片，使用程序化美术。` → 文件夹是空的（默认状态）

## 9. 恢复默认

删掉这个文件夹里的图片即可回到程序化美术，不需要改任何代码。
