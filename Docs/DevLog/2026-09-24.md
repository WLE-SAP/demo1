# 2026-09-24 · 开发日志

## 10:15 ~ 10:40 · 图片资源改成「一物一图」，开始界面背景与按钮也支持图片（无图片时程序绘制）

**目标**：用户要求「修改提供图片资源的方式，减少复用，采取一物一图的方式，同时主界面的背景和按钮也需要兼容图片（无图片时使用程序绘制）」。
旧方案是**按原素材名**匹配：一张 `rect.png` 同时决定房屋屋顶/墙体/门窗 + 木箱 + 村民身子 + 农田垄 + 围栏，
一张 `disc.png` 同时决定树冠 + 村民头 + 羊 + 水井 + 花 + 路灯 + 地洞 + 各种高亮底衬 ——
美术想单独换其中任何一个都做不到；而且开始界面完全不受这套系统影响。

**做了什么**
- **`ArtOverride` 重写为 key 表（一物一图）**：新增 `ArtKeys`（52 个常量）+ `Slots`
  （每个 key 记「原素材 / 是否保留代码染色 `tint` / 是否九宫格 `sliced`」）。
  接口改为 `Get / Has / Or / Apply(SpriteRenderer, key) / ApplyAll / ResolveKey / NormalizeName`；
  一个 key 放多张图会**只用先载入的**并在 Console 点名；名字没对上 key 的图片列进 `UnmatchedFiles`。
  **旧的按素材名匹配（`rect` / `disc` / `roundrect` / `marker` / `grass`）已删除**。
- **染色规则**：套用图片时默认把渲染器颜色刷成白色（图片原样显示）；
  只有颜色本身承载信息的 key 保留染色（村民身/头 = 职业色与肤色、窗/路灯/炉火 = 昼夜变色、
  高亮底衬、花与篷布的随机配色、界面面板与按钮）。规则写在 `Slot.tint` 里，逐 key 可查。
- **`VillageGenerator` 每个物件挂自己的 key**：`AddRect` / `AddDisc` / `AddSlice` 末尾新增 `key` 参数，
  46 处调用点逐个写明 key（屋顶/墙/门/窗/招牌/烟囱/铁砧/炉火/村民身头/树冠/果子/叶子/神奇果实/
  农田土地与垄与幼苗/牧场草地/围栏/羊身与羊头/花坛土与边与花/井圈与水与柱/摊位柜台与篷与柱与货物/
  长椅坐板与靠背/公告板柱与板与纸/路灯柱与灯头/地洞边洞深处石头/木箱）。
  **组合件在已提供整套图片时跳过程序化细节**：有 `tree_canopy` 就不再叠「内部高光」，
  有 `special_food` 就不再叠内部亮斑与小星星（外圈光晕保留）。
  套用发生在**物件创建的那一刻**，所以边走边生成的区块同样生效（旧实现只在开局套一次场景精灵）。
- **场景里手工摆的精灵**新增 `ArtSlot` 组件标 key，并已挂到 `BugScene` 的 `Bug/Head`（`bug_head`）、
  `MoveMarker`（`crosshair`）、`Environment/Ground`（`ground`）三个对象上。
- **`Highlighter.Setup(Sprite)` → `Setup(string artKey, Sprite fallback)`**：底衬也走 key。
- **开始界面新增 `MenuArt`**（挂在 `MainMenu.scene` 的 `Canvas` 上）：读取
  `menu_background` / `menu_panel` / `menu_button` 三个 key 套到背景、3 个面板底板、13 个按钮上；
  **没有图片就自己用代码画** —— 背景是纵向渐变（128px 高的贴图拉伸铺满），
  面板与按钮是 64px 圆角矩形九宫格（圆角 20px、边框 20px），再乘上各控件原本的颜色，
  所以「没有素材」和「有素材」两种状态下界面都不会空白。生成的贴图 `HideAndDontSave`，销毁时倒序释放。
- **导入后处理 `ArtOverridePostprocessor`**：按新 key 表反推 PPU（原素材世界宽度），
  并且**九宫格 key 会自动把边框按原素材的圆角比例放大到新图片上**（旧实现只设了尺寸、没设边框，
  文档里说的「四角 20px 不变形」其实一直没生效）。
- 文档同步：根 `README.md`（第 0 / 1.2 / 1.3 / 1.4 / 1.5 节、交付清单、排错表、实现位置表）、
  `Assets/Resources/ArtOverride/README.md`（整篇重写为 key 表）、`Docs/DevLog/Spec.md`
  （§4.1 素材行、§5 资源规范、新增红线 15）。

**涉及文件**
- 新增：`Assets/Scripts/ArtSlot.cs`、`Assets/Scripts/MenuArt.cs`
- 修改：`Assets/Scripts/ArtOverride.cs`（重写：key 表 + Slot + ArtKeys）
- 修改：`Assets/Scripts/ArtOverrideApplier.cs`（按新接口套用/打日志）
- 修改：`Assets/Scripts/Highlighter.cs`（`Setup(key, fallback)`）
- 修改：`Assets/Scripts/VillageGenerator.cs`（三个工厂加 `key` 参数 + 46 处调用点 + 组合件跳过）
- 修改：`Assets/Scripts/Editor/ArtOverridePostprocessor.cs`（按 key 算 PPU + 自动九宫格边框）
- 修改：`Assets/Scenes/BugScene.scene`（`Bug/Head`、`MoveMarker`、`Environment/Ground` 各加 `ArtSlot`）
- 修改：`Assets/Scenes/MainMenu.scene`（`Canvas` 加 `MenuArt`）
- 修改：`README.md`、`Assets/Resources/ArtOverride/README.md`、`Docs/DevLog/Spec.md`

**验证**
- `Spec.md` 7.1 的 Roslyn 编译校验（`dotnet ... csc.dll @csc.rsp`）：**EXIT=0 且生成 verify.dll**，无 error。
  （中途修掉两处 `CS0170`：`bool known = key != null && Slots.TryGetValue(key, out slot);` 的短路
  会让 `slot` / `info` 可能未赋值，改成先 `= default`。）
- 编辑器 `unity_editor.refresh`：0 error、0 warning。
- **临时测试图端到端验证**（生成 4 张纯色 PNG 放进 `ArtOverride` 再删除）：
  - 导入设置：`ground.png` 256px → PPU 64（4×4，与 `T_Grass` 等宽）；`bug_head.png` 128px → PPU 128（1×1）；
    `menu_background.png` 512px → PPU 128；`menu_button.png` 64px → **边框自动 20px** ✅
  - `BugScene` 运行：`Environment/Ground.sprite.name == "ground"`、`Bug/Head.sprite.name == "bug_head"`，
    color 均为白；场景 1359 个 SpriteRenderer 里**只有这 2 个被替换**，
    `Canopy` 仍是 `S_Disc`、`Roof`/`Body` 仍是 `S_Rect`、`Road` 仍是 `T_Road` ✅（一物一图没有连坐）
  - `MainMenu` 运行：背景 = `menu_background`（Simple、白）；13 个按钮 = `menu_button`（Sliced、边框 20）
    且各自颜色保留（开始=绿 0.55/0.75/0.42、继续=蓝 0.46/0.62/0.74、设置=0.72/0.62/0.44…）；
    3 个面板走**程序化圆角矩形**（边框 20）；`Vignette` 未被误改 ✅
  - **删掉图片后的兜底**：`ArtOverride.Count == 0`；开始界面背景变成程序绘制的 **1×128 渐变**
    （底部 RGBA 0.380/0.471/0.322 = `gradientBottom`、顶部 0.149/0.212/0.161 = `gradientTop`、中间为插值），
    面板/按钮变成程序化圆角矩形（读贴图像素：角上 (0,0)(1,1)(3,3)(5,5) alpha=0，从 (8,8) 起 alpha=1，
    边缘列 (0,32) alpha=1 —— 圆角半径 20px 的剖面正确）；
    背景矩形 2400×1400 > 画布 1920×1080（铺满屏幕）；
    `BugScene` 恢复 `T_Grass` / `S_BugHead` 与全部 `S_Disc` / `S_Rect` ✅
  - 目录清理：删除后 `Assets/Resources/ArtOverride/` 只剩 `README.md`（含 `.meta`），无残留测试图。
- **未做视觉验证**：`analyze_multimedia` 返回 402 `Budget has been exceeded`（额度用尽），
  所以**没有**用眼睛确认过开始界面的观感；上面的结论全部来自结构化数据（精灵名、颜色、贴图像素 alpha、矩形尺寸）。
  录屏文件留在 `screenshots/GameView_2026-09-24_10-22-38-445.mp4` 供人工查看。

**遗留问题 / 下一步**
- 开始界面的**背景观感变了**：原来是 `T_Grass` 平铺 + 深绿染色，现在没图片时是**纵向渐变**（深浅绿）。
  想回到「草地平铺」的观感，放一张 `menu_background.png` 即可；渐变颜色在 `MenuArt` 的
  `gradientTop` / `gradientBottom` 上（挂在 `MainMenu.scene` 的 `Canvas`）。
- 未做视觉验证（额度受限）→ 需要人工看一眼开始界面与游戏内默认外观。
- `Assets/Sprites/` 里的 `S_Crate` / `S_Fence` 等素材仍未使用（木箱/围栏继续用 `S_Rect` 程序化图元），
  这次没有动默认外观；如果想让默认美术也各用各的，可以把它们接到 `crateSprite` / `fenceSprite` 上。

## 10:30 ~ 10:50 · 加「内容目录」接口：新增食物 / 可交互物品只需 Register 一行

**目标**：用户要求「添加接口让我能够更加方便地添加食物和可交互物品」。
改造前加一种食物要改 `VillageGenerator` 里硬编码的 `BuildFood` / `CreateFood` / `BuildSpecialFood` / `BuildCrates`
（顺带还得记住「果子 66%」这种魔法数字），加一种可搬物品更是没有入口。

**做了什么**
- **新增 `SpawnKit.cs`**：
  - `SpawnRule`（class；`SpawnRule.Pool(20f)` / `SpawnRule.Fixed(1,1,0.45f)` / `new SpawnRule{…}`）——
    一处说清「权重 / 每区块固定数量 / 几率 / 只在村庄或野外 / 占地与留白」；
  - `IContentDefinition`（`Id` / `Spawn` / `Create`）—— 生成器只认这个接口；
  - `SpawnKit.YOrder` —— 俯视排序公式的**唯一定义**（`VillageGenerator.YOrder` 改为转调它）。
- **新增 `ArtShapes.cs`**：`ArtShape`（Round/Rect/RoundRect/Berry/Leaf）+ 精灵登记表
  （生成器 Awake 里 `ArtShapes.Use(disc, rect, roundRect, berry, leaf)`）+ `AddSprite`（拼组合外观用）。
- **新增 `FoodCatalog.cs`**：`FoodDefinition`（外观 / 吃 / 悬浮窗 / 生成 / `decorate` 回调）+ `FoodIds` +
  `FoodCatalog`（`Register` / `Find` / `Unregister` / `SetChance` / `Reset` / `EnsureDefaults` / `Create`），
  内置 `berry` / `leaf` / `special_food`（数值与改造前一一对应）。
- **新增 `ItemCatalog.cs`**：`ItemDefinition`（随机大小 / 九宫格 / 可搬 / 可吃 / YSort / 生成 / `decorate`）+
  `ItemIds` + `ItemCatalog`，内置 `crate`。
- **`VillageGenerator`**：删掉 `BuildFood` / `CreateFood` / `BuildSpecialFood` / `CreateSpecialFood` /
  `BuildCrates` / `CreateCrate`，换成目录驱动的 `BuildFoods` / `BuildItems` + 泛型
  `BuildSlots`（位抽签）/ `BuildFixed`（固定数量）/ `Place`（找空地并占位）；
  字段 `foodMin/foodMax` → `foodSlotsMin/Max`、`crateMax` → `itemSlotsMin/Max`，删除 `specialFoodChance`
  （改由 `special_food` 定义的 `chance` + 地图类型调 `FoodCatalog.SetChance`）。
  物体结构统一成「**逻辑在根上、外观在 Visual 子物体上**」（木箱原本如此，食物改成一致），
  于是 `decorate` 里写的光晕 / 星星按世界单位即可，不会被父级缩放二次放大。
- **`MapProfiles`**：三张地图改设 `foodSlots*` / `itemSlots*` + `FoodCatalog.SetChance(FoodIds.SpecialFood, …)`，
  数值与改造前一致（荒野 7~11 / 0~1 / 0.5；城市 8~12 / 0~3 / 0.5；农村 5~8 / 0~2 / 0.45）。
- 文档：`Spec.md` 新增 §4.5「新增内容：食物与可交互物品（内容目录）」并更新 §4.1 生成器一行；
  根 `README.md` §5 补实现位置与「加一种新食物 / 新物品」的说明。

**涉及文件**
- 新增：`Assets/Scripts/SpawnKit.cs`、`Assets/Scripts/ArtShapes.cs`、`Assets/Scripts/FoodCatalog.cs`、`Assets/Scripts/ItemCatalog.cs`
- 修改：`Assets/Scripts/VillageGenerator.cs`（目录驱动 + 字段改名 + Awake 登记形状与目录）
- 修改：`Assets/Scripts/MapProfiles.cs`（foodSlots / itemSlots / SetChance）
- 修改：`README.md`、`Docs/DevLog/Spec.md`

**验证**
- Roslyn 编译校验：**EXIT=0 且生成 verify.dll**；编辑器 `unity_editor.refresh`：0 error / 0 warning。
- Play 模式（`BugScene`）**真实注册新内容后验证**（`FoodCatalog.Register` + `ItemCatalog.Register` + `world.Rebuild()`）：
  - 新内容真的刷出来：`mushroom×89`、`barrel×8`（9 个区块；那局是城市档，8~12 个食物位）；
  - 内置内容抽查：`berry`（nutrition 1、satiety −1 用分量 9、requiredLevel 0、sprite `S_FoodBerry`、有高亮）、
    `special_food`（growth 1、分量 30、trigger 半径 1.08、子物体 = `Visual+Glow+Halo+Inner+SparkA+SparkB`）、
    `crate`（requiredLevel 2、Draggable / YSort / Rigidbody2D / BoxCollider2D 齐全）；
  - 新食物：`Edible.nutrition=2`、`satiety=20`（恢复 20）、`EntityInfo=蘑菇（食物）`、sprite `S_Disc`、在 `Edible.All` 里；
  - **吃的通路**：把小虫放到蘑菇上 → `BugEat.FindTarget()=mushroom`、`CanEat=True`；
  - **捡的通路**：木箱放在小虫正前方 0.8 → `FindInteractable=crate`，按 F → `IsHolding=True` / `IsDragging=True` / 手里距离 0.70，
    放下后状态复原；等级门槛 `requiredLevel=2` vs 小虫 `DisplayLevel=1` → `CanEat=False`；
  - **`SpawnRule.Fixed` 路径**：`SetChance(special_food,1)` 后 `special_food×9`（每区块 1 个）；
  - 接口自检：`Find` / `SetChance` / `Unregister`（卸载后目录回到默认三种）都正常。
- **未做视觉验证**：`analyze_multimedia` 仍报 402（额度用尽），新内容的外观只看过结构化数据
  （sprite 名、子物体列表、组件字段），没有用眼睛确认。
- 过程中 Console 出现过一次 `UnassignedReferenceException: m_AtlasTexture of TMP_FontAsset has not been assigned`
  （TMP 动态图集扩容时的一次性异常，**与本次改动无关**）：随后结构化检查 4 个 TMP 文本
  （HelpText 115 字 / 92 可见、StatusText 53 字 / 40 可见、Title、Body）字形全部正常，中文没变方块。

**遗留问题 / 下一步**
- 世界布局会变：`BuildChunk` 里的生成顺序由「木箱 → 食物 → 神奇果实」改为「房子 → 食物 → 物品」，
  同一个种子刷出来的村庄与改造前不完全一样（存档只存种子与玩家进度，不会坏档，只是布局变）；
  食物落位半径 / 占位改由 `SpawnRule.clearance/footprint` 决定，默认值与改造前一致。
- 未做视觉验证（额度受限）→ 需要人工看一眼新内容外观（测试用的蘑菇是圆形、木桶是圆角方块）。
- 目前的口子：`decorate`（拼组合外观 / 加组件）、`edible` / `draggable` 自由组合（可搬不可吃、可吃不可搬）、
  `wildOnly` / `hamletOnly`（只在某类区块刷）。**还没有「可交互但既不搬也不吃」的行为**（拉杆、祭台之类）——
  需要时在 `decorate` 里挂自己的脚本组件即可，但届时应把这类「交互」抽成正式约定再写进 Spec。

## 10:55 ~ 11:20 · HUD 改版：面板不遮挡、体力条移到底部居中、等级越高条越长

**目标**：用户要求「修改游戏内 hud，不要遮挡，血条移到屏幕正下方，等级越高血条越长」。

**做了什么**
- **体力条挪到屏幕正下方居中**：`SimpleHUD.LayoutPanels` 里把它的锚点设成
  `anchorMin = anchorMax = (0.5, 0)`、`pivot = (0.5, 0)`、`anchoredPosition = (0, staminaBottomMargin)`，
  不再跟在左上角两块面板下面（原来它在「说明 → 状态」之后，是三块里最下面那块）。
  Fill 在底衬里是左对齐的（`pivot.x = 0`），所以条变长时从中间往两边长、消耗时仍然从右往左退。
- **等级越高血条越长**：条长 = `staminaBarWidth`（初始 460）+ `level × staminaWidthPerLevel`（默认 110），
  即 0/1/2/3 级 → 460 / 570 / 680 / 790（画布单位）；长大 = 体力上限更高，条也跟着变长才看得出来。
  等级没变就不改 RectTransform（`lastBarLevel` 缓存），避免每帧触发重排。
  Fill 的宽度 = (条长 − 内缩×2) × 体力比例，内缩与高度在 `Awake` 里从场景读出来，不用手填。
- **面板不再遮挡**：`LayoutPanels` 现在先按**文字的实际高度**收面板（`FitPanel`：`TMP.preferredHeight + 边距`，
  说明边距 28 / 状态边距 20），再按实际高度从上往下排；状态行文字换行 / 变长时（`Update` 里比较
  `preferredHeight`）重新排一次，所以面板永远包得住文字、也不会压住下一块。
  实测原来「操作说明」面板高 262 而文字实际要 311 —— **文字是溢出的**，现在面板 339 = 311 + 28，刚好包住。
- 顺带修掉一个会让上面整套逻辑失效的崩溃：`SimpleHUD.Awake` 里取初值时会读
  `BugController.NearbyBurrow`，而它在 `BugController.Awake` 里用 `rb.position`；HUD 比小虫先跑时
  `rb` 还是 null → `NullReferenceException`，整个 `Awake` 从中断处往下都不执行（版式全没生效）。
  已改为 `rb != null ? rb.position : (Vector2)transform.position`（写进 Spec 红线 16）。

**涉及文件**
- 修改：`Assets/Scripts/SimpleHUD.cs`（体力条锚点 / 长度随等级 / `FitPanel` 版式 / Awake 初值）
- 修改：`Assets/Scripts/BugController.cs`（`NearbyBurrow` 兜底，组件顺序安全）
- 修改：`Docs/DevLog/Spec.md`（§4.1 界面一行 + 新增红线 16）

**验证**
- Roslyn 编译校验：**EXIT=0**；编辑器 `unity_editor.refresh`：0 error / 0 warning。
- Play 模式（`BugScene`，画布 1280×720 → scaleFactor 0.667）结构化测量：
  - 体力条锚点 `(0.5,0)/(0.5,0)`、pivot `(0.5,0)`、`anchoredPosition=(0,40)` ✅ 在屏幕正下方；
  - 长度随等级：0 级 460 → 1 级 570 → 2 级 680 → 3 级 790，**屏幕中心始终 = 画布中心 640** ✅；
    底边 y 27~44 px（≈ 离底 40 画布单位 × 0.667）✅；
  - Fill 宽度跟着条长走：满级 790 时 Fill 773（= 790 − 8，体力 99/100 → 乘比例）✅；
  - **遮挡检查**（满级、条最长时）：`HelpPanel`(x16~389,y478~704)、`StatusPanel`(x16~389,y411~471)、
    `StaminaBar`(x377~903,y27~44)、`EncounterWindow`(x893~1253,y113~213) —— **两两都不相交** ✅
    （条最长时与右下悬浮窗 x 有 10px 重叠区，但 y 完全错开）；
  - 面板高 = 文字高 + 边距：HelpPanel 339 = 311 + 28、StatusPanel 90 = 70 + 20，两块间距 = 10 ✅。
- **未做视觉验证**：`analyze_multimedia` 仍报 402（额度用尽），只做了矩形/尺寸的结构化测量，
  没有用眼睛确认观感（配色、粗细、位置是否好看）。

**遗留问题 / 下一步**
- 左上角仍然是「操作说明 + 状态行」两块常驻面板（720p 下约 373×226 px）。如果还嫌占画面，
  可以再做成：说明面板可折叠 / 只在按 H 时显示，或整体缩字号 —— 这属于观感取舍，等用户定。
- 未做视觉验证（额度受限）→ 需要人工看一眼底部体力条的位置与长度是否合适
  （`SimpleHUD` 的 `staminaBottomMargin` 40、`staminaBarWidth` 460、`staminaWidthPerLevel` 110 都可调）。
- 体力条现在的长度按「等级」算；如果以后改成按 `maxStamina` 算（等级之外还有别的加体力上限的途径），
  改 `SimpleHUD.BarWidth` 一处即可。

## 23:15 ~ 23:40 · 步骤①：事件总线 + 声音传播 + 村民「调查 / 搜索」AI

**目标**：按《游戏核心玩法与机制设计》§5.2（NPC 状态机）、§6（被发现机制）、§9/§19.2（连锁 + 事件系统），
补上主循环里断掉的一环：**小虫搞了事，村民完全不知道**（原来只有「正面看到小虫」一条通路，
没有声音、没有调查、没有搜索）。用户确认：从这一步开始做，场景方向是「保留村庄 + 以后做可进入的室内房屋」，
先出计划、确认后再写代码（计划见会话计划文件）。

**做了什么**
- **新增 `GameEvent.cs`（全局事件总线）**：`NoiseKind{Step,Dash,Eat,Pickup,Drop,Break,Shock}`、
  `NoiseEvent{position,loudness,kind}`、`EatenEvent{food,position,wasVillager}`，
  四个静态事件 `Noise` / `Eaten` / `Spotted` / `StateChanged` + `Raise*`（内部判空、struct 载荷零分配）；
  默认响度常量 `StepLoudness 0.35 / DashLoudness 0.6 / EatLoudness 0.5 / PickupLoudness 0.5 /
  DropLoudness 1.2 / BreakLoudness 2 / ShockLoudness 2.5` 与 `LoudnessOf(kind)`；
  `Reset()`（`RuntimeInitializeOnLoadMethod(SubsystemRegistration)`）清空订阅表；诊断用 `NoiseRaised` /
  `EatenRaised` / `LastNoise` / `NoiseSubscriberCount`（`GetInvocationList().Length`）。
- **噪音产生点接进 5 个现有动作**：`BugFootsteps`（每步 0.35）、`BugController`（冲刺 0.6，期间每 0.25s 一次，
  `TryDash` 里把 `nextDashNoiseTime` 归零让首次立刻发声）、`BugEat`（吞下 0.5，**吃村民用 1.2**）、
  `DragController`（拾取 0.5 / 放下 1.2，放下前先把位置记下来）。
- **`Villager` 加听觉与 4 个新状态**：
  - 新增 `Alert` / `Investigate` / `Search` / `Recover`，与原有的 `Chase` / `Flee` 一起接成
    「听到动静 → 起疑 → 走过去张望 → 回过神」与「追丢 → 在最后看见的地方附近翻找 → 回过神」两条链；
    走动复用现有 `CommuteTo/Arrive`（不引入寻路），卡住沿用 `stuckTimeout` 当作到达。
  - 听觉：`OnNoise` 只记字段（位置/响度）不改状态，真正的状态切换交给 `Decide()`；
    半径 = `hearingBase`(6) × 响度 × `VillagerJobs.Curiosity(job)`，**全向**（不是视野扇形）；
    距离 ≤ 半径一半记准确位置、否则加随机抖动；`hearingCooldown`(1s) 节流；
    正在追/躲、被冻结、`fearsBug` 的人不响应。
  - 订阅/退订在 `OnEnable` / `OnDisable`（冻结与区块回收都会走 `OnDisable`）。
  - 头顶「!」提示：`Awake` 里用 `ArtShapes.Get(ArtShape.Rect)` 拼一根竖条 + 一个点（明黄，
    `sortingOrder 6`、挂在根物体上、`localPosition y 0.62`），**不占美术 key**；
    只在这三个状态（含「正走过去」的 `Commute`）显示，冻结时随其它渲染器一起关掉、解冻时刷新。
  - `SetState` / `Arrive` / `CommuteTo` 里广播 `StateChanged`。
- **`VillagerJobs`**：新增 `Curiosity(job)`（守卫 1.6 / 孩子 1.5 / 樵夫 1.2 / 摊贩·面包师·牧羊人 1.0 /
  长者 0.8 / 农夫 0.6 / 铁匠 0.5）与 `InvestigateText(job)`（调查时的动作文案，接到 `ActivityText`）。
  `Villager.leavePostThreshold`（默认 1.0）以下的职业**只原地起疑、不离开岗位**。
- **`Villager` 的行为修复（顺带，但是必须的）**：视野检查原来只在 `Decide()` 里做
  （到点重新做决定，最长十几秒），所以**正在赶路的村民会径直从小虫身上走过去当作没看见**；
  现在 `Update` 里对所有状态（正在追/躲的除外）都查一次 `CanSeeBug()` → 「看到小虫优先于一切日常安排」
  这条注释里的约定才真正成立。同时把 `Chase` / `Flee` 的倒计时接上（原来这两个状态直接 `return`、
  `stateTimer` 永不递减 → 追到天荒地老也不会「失去目标」），现在追丢会走 `Search`。
- **`SimpleHUD`**：状态行新增一句提示（`NoticeHint()`，`noticeRadius` 12）：
  「有人听到动静，正朝这边过来（N 个）」/「附近有人听到了动静（N 个）」；
  只追加文字、不新增面板，版式仍由 `LayoutPanels` 按文字高度自动收。

**涉及文件**
- 新增：`Assets/Scripts/GameEvent.cs`
- 修改：`Assets/Scripts/Villager.cs`（4 个新状态 + 听觉 + 头顶提示 + 视野检查前置 + 追丢转搜索）
- 修改：`Assets/Scripts/VillagerJobs.cs`（`Curiosity` / `InvestigateText`）
- 修改：`Assets/Scripts/BugFootsteps.cs`、`BugController.cs`、`BugEat.cs`、`DragController.cs`（发声）
- 修改：`Assets/Scripts/SimpleHUD.cs`（提示行）
- 修改：`Docs/DevLog/Spec.md`（§4.1 表格、新增 §4.6、新增红线 17/18/19）
- 未动场景 / 预制体 / `.meta`、未动美术与音频 key（本次不新增素材）

**验证**
- **编译校验**（Spec §7.1 的 Roslyn 脚本）：`dotnet … csc.dll @csc.rsp` → **EXIT=0 且生成 verify.dll**（多轮，含最后一次）。
- 编辑器 `unity_editor.refresh`：**0 error / 0 warning**（多次）。
- **Play 模式结构化断言**（`exec_runtime_script`，`BugScene`，固定 `LoadWorld(20260922, MapKind.Village)`），
  10 条全部通过：
  1. 噪音传播：村民脚下 0.9r 处响一声 → `HasPendingNoise=True`、`Decide()` 后 `State=Alert` ✅
  2. 半径公式：守卫 `HearingRadius(0.5)=4.80` = 6×0.5×1.6；脚步 3.36 / 木箱 11.52；1.1r(5.28) 处听不到 ✅
  3. 状态链：`Alert → Commute(走过去) → Investigate(张望，最终离声源 0.16) → Recover → 回到日常` ✅
  4. 职业差异：农夫（好奇 0.60）只 `Alert`，位移 0.00、不进入 `Investigate` ✅
  5. 节流：冷却期内贴脸再响一声**不记**；冷却过后 0.9r 再响**又能听到** ✅
  6. 追丢→翻找：把会追的村民引到小虫旁 → `Chase`；`SetHidden(true)` 后 → `Search`（`ActivityText=在附近翻找小虫`）→ 回过神 ✅
  7. 冻结：`SetFrozen(true)` 后订阅者 23→22、`enabled=false`，连 5.0 响度的噪音也不记 ✅
  8. 五种动作都出声：脚步 Step、冲刺 Dash、吃 Eat(0.5)、拾取 Pickup(0.5)、放下 Drop(1.2) 分别验证到 ✅
  9. **事件无泄漏（关键回归）**：`world.Rebuild()` 两次后 `NoiseSubscriberCount == Villager.All` 有效对象数
     （23 == 23）；跑完全部用例后复检 23 == 23 ✅
  10. 吃村民：等级调到 4 级吃掉受害者（已消失）→ 现场目击者 `fearsBug False→True`（日志
      `[Villager] 胡大牛（守卫）亲眼看到小虫吃人…`）→ 平静后听到 2.5 响度噪音只 `Alert`、**不去查看** ✅
  11. HUD：状态行确实追加了「附近有人听到了动静（1 个）」；面板矩形两两不相交（说明 373×226 / 状态 373×60 / 体力条 y 26.67）✅
- **未做视觉验证**：`analyze_multimedia` 本次未调用（此前多次报 402 额度用尽），
  顶部「!」提示与 HUD 观感**没有用眼睛确认过**，上面的结论全部来自结构化数据
  （状态枚举、字段值、世界坐标、订阅者计数、TMP `textInfo`、UI 矩形）。

**遗留问题 / 下一步**
- **发现一处既有字体缺陷（本次暴露，非本次引入）**：`Assets/Codely/Fonts/` 的
  `NotoSansSC-Regular SDF` 声明了 **2 张 atlas texture 但只有第 1 张有内容、且第 1 张已写满**，
  于是**任何新中文字形**都会在 `TMP_FontAsset.TryAddCharacterInternal` 抛
  `UnassignedReferenceException: The variable m_AtlasTextures of TMP_FontAsset has not been assigned`，
  那一帧的 `ForceMeshUpdate` 被中断 → 新加的中文文案可能不显示 / 漏字。
  实测证据：`atlasTextures.Length=2 非空=1`、`main.HasCharacter(c, true, true)` 直接抛出该异常、
  渲染后 `textInfo` 里出现 1 个不可见字形。**修复要走 skill「TMPChineseFont」（需要用户授权）**，
  否则后续所有新增中文文案都会踩这个坑（已记入 Spec 红线 19）。**本次未修**，等用户决定。
- 未做视觉验证（额度受限）→ 需要人工看一眼：村民头顶的「!」、HUD 新增那行提示的位置与观感。
- 数值待手感确认（都可调）：`hearingBase` 6、`hearingCooldown` 1s、`step/drop` 响度、`searchPoints` 4、
  `searchRadius` 3.5、`noticeRadius` 12、各职业 `Curiosity`。
- 村民调查是**直线走过去**（没有寻路），被房子挡住时靠 `stuckTimeout` 收场；
  以后如果要「绕开房子去查看」，需要给村民加简易寻路（本次没做）。
- 下一步（按计划顺序）：② 能力系统（吃电池/化学品等获得电击、腐蚀、分裂、伪装、抖动）——
  能力的事件与噪音信道已经在 `GameEvent` 里留好（`Break` / `Shock` 两个 `NoiseKind` 与默认响度已定义）。

## 23:35 ~ 23:50 · 修掉字体缺陷：Noto SC 图集索引卡在空槽上（用户已授权）

**目标**：上面遗留问题①，用户授权修复。不修的话步骤②的新中文文案会继续漏字。

**定位（读 TMP 3.0.9 源码 + 编辑器诊断，不是猜的）**
- `TMP_FontAsset` 里 `m_AtlasTextures`（`[SerializeField] Texture2D[]`）与 `m_AtlasTextureIndex`（`[SerializeField] internal int`）
  都是**序列化**的，而 `atlasTextureCount` 就是 `m_AtlasTextureIndex + 1`。
- 实测资产状态：`atlasTextures.Length = 2`、只有槽 0 非空（2048×2048）、`atlasTextureCount = 2` → **索引卡在 1**。
- 源码 `SetupNewAtlasTexture()`（写满时扩容）在**运行时**新建贴图并 `AddObjectToAsset`；只要这次扩容没被存进资产，
  磁盘上就会留下「索引=1 但槽 1 是 null」。之后任何新字形都会在 `TryAddCharacterInternal` 第 2063 行
  `m_AtlasTextures[m_AtlasTextureIndex].isReadable` 处抛 `UnassignedReferenceException`。
- 另外：动态字体的 `m_UsedGlyphRects/m_FreeGlyphRects` 也是序列化的，扩容时会**清空重来** →
  这时就不能只把索引改回 0（那会让新字形从零开始覆盖旧字形的像素），必须连字形表一起清。

**做了什么**
- skill「TMPChineseFont」步骤 1/2：TMP Essential Resources 已在（`TMP_ESSENTIALS_READY imported=false`），
  中文 fallback 也已配好（skill 报 `TMP_CHINESE_FONT_READY`）—— **但 skill 不检查图集槽位，所以它不会修这个缺陷**。
- 就地修复（保留资产 GUID / 不破坏任何引用）：`fontAsset.ClearFontAssetData(false)`
  → 清空字形表与字符表 + `ClearAtlasTextures` 把图集数组缩回 1 张、索引归零、清像素
  → `SetDirty` + `SaveAssets`。
- **预热常用中文**：`fontAsset.TryAddCharacters(...)` 一次性把 HUD / 村民职业 / 悬浮窗 / 操作说明等 149 个汉字
  写进图集（同时也是「图集还能不能装新字形」的验证）。

**涉及文件**
- 修改：`Assets/Codely/Fonts/NotoSansSC-Regular SDF.asset`（清空并重建图集数据 + 预热 149 字；GUID 不变）
- 修改：`Docs/DevLog/Spec.md`（红线 19 重写为「已修 + 自查 + 修法」）

**验证**
- 编辑器结构数据：修复前 `槽=2 非空=1 atlasTextureCount=2 字形数=383` →
  修复后 `槽=1 非空=1 atlasTextureCount=1 字形数=0` → 预热 `ok=True 缺字=无 字形数=149`。
- **Play 模式压力测试**（故意用 24 个从没在游戏里出现过的生僻字「蝙蝠蟑螂蜈蚣蜥蜴蜗牛蜘蛛蝌蚪蚯蚓蚕蛾蜂蚁蝗螳蟋蟀」）：
  `不可见字形=0（全部可见）`、Noto 字形数 `167 → 191`（24 个新字形成功加进图集）、
  **`logs` 为空（一次异常都没有）**——同一个操作在修复前会抛 2~3 次 `UnassignedReferenceException`。
- 步骤① 的 HUD 提示行复检：`已吃 0 个 · 体力 94/100 · 速度 0.0 ⏎ 附近有人听到了动静（1 个） ⏎ [F] …`
  → `不可见字形=0`；村民头顶「AlertMark」存在且 2 个渲染器、起疑时显示 ✅（修复前这里会漏 1 个字）。

**遗留问题 / 下一步**
- 只有 149 个常用字被预热；其余字符仍是运行时按需栅格化（现在这条通路已经正常）。
  如果以后想让编辑器里就有全量字形，可以再跑一次更大范围的 `TryAddCharacters`（或对场景做一次「Update Atlas Texture」）。
- 下一步：步骤② 能力系统。

## 23:50 ~ 00:35 · 步骤②：能力系统（吃物品 → 获得能力，4 种能力 + 存档 v3）

**目标**：按设计文档 §2.1 / §4 / §13 与 §24 原则二（「玩家不要只获得数字奖励，要获得新玩法」），
把「吃东西」从「长大 / 回体力」升级成**解锁能力**。用户确认过计划（腐蚀留到「可破坏物体」那一步）。

**做了什么**
- **新增 `Ability.cs`**：`AbilityId`（电击 / 伪装 / 分裂 / 抖动 + 留位的腐蚀）、`AbilityInfo`（名字 / 说明 / 键 / 是否可玩）、
  `Abilities`（表 + `MaxCharges`=3 + `Playable` + `KeyName`）、`AbilityPickup`（挂在食物上的「吃到就给能力」标签）。
- **新增 `AbilitySet.cs`**（自动挂到小虫身上，四个效果都在这里）：
  - 充能 / 冷却 / `TryUse`（没解锁、没充能、冷却中、躲在地洞里都会拒绝并说明原因）；
  - **电击**：半径内村民 `Stun()`，附近 `NightGlow.Flicker()`，广播 `Shock`(2.5) 噪音；
  - **伪装**：`disguiseUntil` + 身上盖一层半透明「破烂」（延迟创建，等 `ArtShapes` 登记好）；
  - **分裂**：放 2 只 `Decoy`（乱窜 + 每 0.7s 发 0.6 响度噪音）；
  - **抖动**：把附近 `Draggable` 震飞 + 附近村民 `Panic()` + `Break`(2.0) 噪音 + 灯闪；
  - `HudLine()` / `SaveMask()` / `SaveCharges()` / `Restore()`。
- **新增 `Decoy.cs`**：假小虫（游走 + 定期出声 + 到时缩小消失），外观用 `ArtShapes` 程序化拼，**不占美术 key**。
- **`Villager`**：新状态 `Stunned` + `Stun(秒)` + `Panic(秒)`；`CanSeeBug()` 尊重伪装
  （判定放在**贴脸分支之前**，否则伪装只在尾巴上生效）；被电麻时看不见也听不见。
  另外新增「**听到动静当场打断闲事**」：`OnNoise` 里如果这个人正在 发呆/干活/闲聊/玩/赶路，立刻 `Decide()`
  —— 不打断的话动静要等它手上的活干完（最长十几秒）才处理，分身引不动人。
- **`NightGlow.Flicker(秒)`**：期间颜色在「亮 / 灭」之间用正弦 + 柏林噪声乱跳，结束后回到按昼夜算的颜色。
- **`GameEvent`**：新增 `AbilityUsed` 事件 + `AbilityUseCount`（并进了 `Reset()`）。
- **`GameInput`**：数字键 1~5（`AbilityKeys` / `AbilityDown` / `AbilityName`）。
- **`FoodCatalog`**：`FoodDefinition` 加 `hasAbility / ability / abilityCharges`；内置 4 样东西
  （`battery` 电击 2 发 / `trash` 伪装 2 发 / `spore` 分裂 1 发 / `gear` 抖动 2 发），
  `SpawnRule.Pool` 只在村庄刷、`requiredLevel = 2`；`spore` 带一颗程序化高光点。
- **`BugEat`**：吞下时 `AbilitySet.GrantFromPickup(food.GetComponent<AbilityPickup>())`。
- **`BugController`**：`IsDisguised` / `IsInvisibleToVillagers`（给村民与 HUD 查）。
- **`SimpleHUD`**：状态行加「能力」一行（只列已解锁的，带键位与剩余充能、冷却秒数、伪装剩余秒数）；
  操作说明加一行「数字键 1~4：使用能力」。
- **存档 v3**：`GameSave` 加 `abilityMask` + `abilityCharges`（长度 5），`AutoSave` 存 / 读；
  **`AutoSave` 改成惰性查找 `AbilitySet`**（见下）。
- **美术 key**：新增 `battery` / `trash` / `spore` / `gear`（`ArtKeys` + `Slots`），
  并按红线 15 同步了根 `README.md`（表格 + 交付清单）与 `Assets/Resources/ArtOverride/README.md`。

**涉及文件**
- 新增：`Assets/Scripts/Ability.cs`、`AbilitySet.cs`、`Decoy.cs`
- 修改：`Villager.cs`、`VillagerJobs.cs`（无）、`NightGlow.cs`、`GameEvent.cs`、`GameInput.cs`、
  `BugController.cs`、`BugEat.cs`、`FoodCatalog.cs`、`SimpleHUD.cs`、`SaveSystem.cs`、`AutoSave.cs`、`ArtOverride.cs`
- 修改：`README.md`、`Assets/Resources/ArtOverride/README.md`、`Docs/DevLog/Spec.md`（§4.1、§4.6 补一句、新增 §4.7、红线 20/21）
- 未动场景 / 预制体 / `.meta`

**验证**
- 编译校验（Spec 7.1）：**EXIT=0 且生成 verify.dll**（多轮）；`unity_editor.refresh`：**0 error / 0 warning**（多次）。
- **Play 模式结构化断言，14 条全部通过**（`BugScene`，固定 `LoadWorld(20260922, Village)`）：
  1. 吃旧电池 → `IsUnlocked(Shock)=True`、充能 `=2` ✅
  2. 用一次 2→1，冷却内再放被挡（还剩 5.5s）✅
  3. 充能清零 → 使用被挡；再吃一个 → 充能回到 2 ✅
  4. 电击麻住半径内村民（`State=Stunned`、看不见小虫、位移 0.00），半径外 12 米的村民不受影响 ✅
  5. 电击时最近的路灯 `IsFlickering=True`、颜色 alpha 0.00→0.57→0.62（白天灯是透明的，所以看 alpha）、
     噪音 `Shock/2.5` ✅
  6. 伪装：4 米处（整圈视野的村民）看不见 → `CanSeeBug=False`；挪到 0.5 米（< awareRadius×0.6=0.84）→ 露馅 `True`；
     身上「Disguise」外观已显示 ✅
  7. 伪装到时自动解除：恢复 4 米可见、外观自动收起 ✅
  8. 分裂：场上分身 `=2`、1.6 秒内广播 6 次噪音、村民听到的位置离最近分身 0.88 米 ✅
  9. 村民被分身引走：之前状态 `Commute` → 听到后 `IsHeadingToNoise=True`（`ActivityText=朝声响走过去了`）✅
  10. 分身到寿自动消失：剩余 `0` ✅
  11. 抖动震飞箱子：推之前箱子**已睡眠**且是 `Kinematic` → 位移 1.96、0.4s 时速度 2.07（当时已切成 `Dynamic`）→
      之后自动收回 `Kinematic` 且速度归零 ✅；**收回后仍能正常 F 搬运**（拾取/放下都成功）✅
  12. 抖动把附近村民变成 `Flee`、广播 `Break/2.0` ✅
  13. 存档 v3：`version=3`、掩码 9→9、充能数组长度 5、往返恢复一致、清空生效、**null 充能不报错（v2 老档路径）** ✅
      （测试前备份了玩家真实存档、测试后原样写回，没有吃掉玩家进度）
  14. 步骤① 的红线回归：噪音订阅者 == 未冻结村民数 ✅
- **未做视觉验证**：`analyze_multimedia` 未调用（额度受限）。伪装那层「破烂」、分身、灯闪、头顶「!」的**观感都没用眼睛确认过**，
  上面的结论全部来自结构化数据（状态枚举、颜色通道、速度/位移、充能计数、TMP 文本、存档 JSON 字段）。

**过程中发现并修掉的两个真 bug（都是测试逼出来的）**
1. **`AutoSave` 在 `Awake` 里缓存 `AbilitySet`，永远是 null**：`AbilitySet` 是场景加载完成后才自挂的，
   于是存档写不出能力，而且 `abilityCharges` 被写成**空数组**（`null` 序列化成 `[]`），读档时索引越界。
   → 改成惰性查找 + 写档时兜底给完整长度数组（红线 21）。
2. **抖动对木箱无效**：木箱是 `Kinematic` 刚体，`AddForce` 完全不起作用（位移 0.00）。
   → 推之前临时切 `Dynamic` + `WakeUp()`，推完用协程收回 `Kinematic`（红线 20）。
   （另外发现：`Villager.OnNoise` 只记字段不改状态，导致「正在赶路的村民听到动静要等活干完才反应」，
   分身根本引不动人 → 改成当场打断闲事。）

**遗留问题 / 下一步**
- **腐蚀**（`AbilityId.Corrode`，表里已留位、`playable=false`）没做：它需要「可破坏物体」才有作用对象。
- 分身的噪音类型现在借用 `NoiseKind.Dash`（响度 0.6），语义上有点将就；等做「连锁 / 破坏」时可以补一个 `NoiseKind.Decoy`。
- 能力数值都还没经过手感调优（电击半径 3.2 / 麻 3.5s / 伪装 8s / 分身 8s / 抖动半径 3.5 + 冲量 5.5），
  都在 `AbilitySet` 的 `[Header]` 字段上，随时可调。
- 只有 4 种能力物品都**只在村庄区块**刷；野外区块吃不到能力（这是刻意的，但需要真机手感确认）。
- 下一步（计划顺序）：③ 可破坏 / 可触发 / 可连接物体 + 连锁，顺带把【腐蚀】和 `NoiseKind.Decoy` 一起补上。
